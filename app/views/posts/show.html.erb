id : <%= @post.id %>
title : <%= @post.title %>
content : <%= @post.content %>
<% if @post.image.attached? %>
    <%= image_tag url_for(@post.image)%>
<% end %>

<a href="/posts/index">리스트 페이지로 가기</a>